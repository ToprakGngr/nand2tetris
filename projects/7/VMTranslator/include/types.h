#ifndef TYPES_H
#define TYPES_H

enum InstructionType {
    PUSH_TYPE,
    POP_TYPE,
    ARITHMETIC_TYPE,
    LABEL_TYPE,
    GOTO_TYPE,
    IF_TYPE,
    FUNCTION_TYPE,
    RETURN_TYPE,
    CALL_TYPE
};

#endif
